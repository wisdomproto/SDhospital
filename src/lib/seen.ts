/**
 * "마지막으로 본 시각" 쿠키들.
 *
 * 읽음 여부를 행마다 저장하지 않는다 — 소식은 전체 공지이고 보호자별 상태를 쌓으면
 * 그 표만 계속 커진다. 마지막으로 화면을 연 시각 하나면 "그 뒤에 올라온 것"을 셀 수 있다.
 *
 * ⚠️ 서버가 읽는 값이므로 상수는 반드시 순수 모듈에 둔다 (`"use client"` 파일에 두면 서버에서 undefined).
 */
export const NEWS_SEEN_COOKIE = "sd-news-seen";
